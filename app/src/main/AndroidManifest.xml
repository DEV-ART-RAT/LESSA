<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.DevRAT.lessa">

    <uses-permission android:name="android.permission.INTERNET">
    </uses-permission>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".UI.Activities.SearchActivity">
        </activity>
        <activity android:name=".UI.Activities.SenaActivity">
        </activity>
        <activity android:name=".UI.Activities.LoadActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".UI.Activities.GoogleSingInActivity">
        </activity>
        <activity
                android:name=".UI.Activities.MainActivity"
                android:label="">
        </activity>
    </application>

</manifest>